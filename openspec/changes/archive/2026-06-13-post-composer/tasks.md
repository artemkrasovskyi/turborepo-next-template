1. Create shared post types and `MAX_POST_LENGTH` constant
2. Create posts client (`createPost`)
3. Create `PostComposer` component
4. Create `createPostAction` server action with validation and revalidation
5. Integrate composer into home page
6. Add character counter and pending/error states to composer
7. Update package exports for the posts feature
