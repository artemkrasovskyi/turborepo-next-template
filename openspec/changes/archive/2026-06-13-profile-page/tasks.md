1. Create shared `ProfileUser` type
2. Create profile client (`getProfileByUsername`, `getProfilePosts`)
3. Update package exports for the profile feature
4. Create `ProfileHeader` component
5. Create `ProfilePosts` component with empty state
6. Create `loadMoreProfilePostsAction` and `ProfileLoadMoreButton`
7. Create `/profile/[username]` page with not-found state
8. Link feed post authors to their profile page
