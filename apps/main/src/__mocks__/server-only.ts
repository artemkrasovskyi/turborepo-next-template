// Test stub for the 'server-only' package.
// The real package throws at runtime when imported in a non-server context.
// This stub is a no-op so server-only modules can be imported in vitest.
export {};
